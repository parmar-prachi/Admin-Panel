<nav class="navbar navbar-expand-lg shadow-sm px-4">

    <!-- Sidebar Toggle -->
    <button class="btn btn-outline-secondary me-3" id="sidebarToggle">
        ☰
    </button>

    <!-- Search -->
    <form class="d-flex" action="/users" method="GET">

        <input class="form-control me-2" type="search" name="search" placeholder="Search users...">

        <button class="btn" type="submit" style="background-color: #6c5dd3; border-radius: 10px; font-weight: 500;">
            Search
        </button>

    </form>

    <div class="ms-auto d-flex align-items-center">

        <!-- Notification -->
        <button class="btn btn-light position-relative me-2">

            <i class="bi bi-bell"></i>

            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">

                3

            </span>

        </button>

        <!-- Messages -->
        <button class="btn btn-light me-2">

            <i class="bi bi-envelope"></i>

        </button>


        <!-- Profile -->
        <div class="dropdown">
            <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle text-white fs-5"
                id="profileDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">

                <i class="bi bi-person-circle ms-2 me-2 fs-3 text-white"></i>

                <div>
                    <span class="fs-5 fw-medium text-white">Administrator</span>
                </div>
            </a>

            <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="profileDropdown">
                <li><a class="dropdown-item" href="/profile">My Profile</a></li>
                <li>
                    <a class="dropdown-item" href="/profile/change-password">
                        Change Password
                    </a>
                </li>
                <li>
                    <hr class="dropdown-divider">
                </li>
                <li><a class="dropdown-item text-danger" href="/logout">Logout</a></li>
            
            </ul>
        </div>

    </div>

</nav>

<style>
    .dropdown-menu-end {
        border: 1px solid rgba(0, 0, 0, 0.05) !important;
        border-radius: 12px !important;
        box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1),
            0 4px 10px -6px rgba(0, 0, 0, 0.05) !important;
        padding: 0.5rem;
        min-width: 200px;

        animation: fadeInDropdown 0.2s ease-out;
    }


    .dropdown-menu-end .dropdown-item {
        border-radius: 8px;
        padding: 0.6rem 1rem;
        font-size: 0.9rem;
        font-weight: 500;
        color: #475569;
        transition: all 0.2s ease;
    }


    .dropdown-menu-end .dropdown-item:hover {
        background-color: #f1f5f9;
        color: #0f172a;
        transform: translateX(3px);
    }


    .dropdown-menu-end .dropdown-item.text-danger:hover {
        background-color: #fef2f2;
        color: #dc2626 !important;
    }


    .dropdown-menu-end .dropdown-divider {
        border-top: 1px solid #f1f5f9;
        margin: 0.5rem 0;
        opacity: 1;
    }

    /* Fade-in keyframes */
    @keyframes fadeInDropdown {
        from {
            opacity: 0;
            transform: translateY(8px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>